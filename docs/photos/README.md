Put politician headshot images here (small, square, e.g. 100x100px JPG/PNG
works well). Reference them in scripts/politicians.json via the "photo"
field as a relative path, e.g. "photos/mayor.jpg".

If a photo is missing or fails to load, the UI just hides the image
silently — it won't break anything.
